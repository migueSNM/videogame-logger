# Supabase — required for real "Sign in" (email/password + Google).
# "Try it for free" (trial mode) works without these, entirely in localStorage.
NEXT_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=xxxx
